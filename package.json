{
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "scripts": {
    "client": "npm --prefix my-app run dev",
    "server": "npm --prefix backend start",
    "dev": "concurrently \"npm --prefix backend start\" \"npm --prefix my-app run dev\"",
    "build": "npm --prefix my-app run build"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.2",
    "realm-web": "^1.5.1",
    "rollup": "^2.79.2"
  }
}
